export default function Footer() {
    let bemBlock = `footer__`;
    return (
        <footer className = "footer">
            <div className = {`${bemBlock}container container`}>
                <h1>My App Header</h1>
            </div>
        </footer>
    );
}